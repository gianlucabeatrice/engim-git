
n=leggiilnumero(); //leggiilnumero è la funzione nativa per accettare un num digitato da tastiera

int out=0; //sarà il numero da restituire alla funzione


for (int i=1; i<=n; i++)
    (
         //per ogni numero verifico se è primo, in tal caso incremento out

         if (ilnumeroprimo(i))
                 out=out+1;
   )
scrivi(out);


bool valore= ilnumeroeprimo (5);
int j=9;
bool valore2=ilnumeroeprimo(j);
if (ilnumeroeprimo(j))
     scrivi('ok');


//fine programma main


//statement di dichiarazione variabile

tipo nomevar=inizializzazione

function tipo_di_output nomefunzione (parametri di ingresso)
(
     //blocco di programma

     return tipo_di_output
)


function bool ilnumeroeprimo(int n)
(
    //n la uso solo dentro il blocco della funzione
    //NON c'entra nulla con altre variabili all'esterno
    //SCOPE di n è il blocco della function

    //la funzione restituisce true se il numero è primo, false altrimenti

    //il numero è primo se e solo se è divisibile per 1 e se stesso
    //il numero NON è primo se trovo un divisore tra 2 e n-1 che lo divide

)

bool ret;

int var1=n;
int calc;
if(n<0) //gestire errore
else if (n=0) //gestire errore
else if (n=1)
         ret=true;
for (int num=2; n<var1;num++)
(calc=var1/num; // 1=5/3)
if( calc*num i=var1) // 1*3 I=5
//non fa nulla
else
//il numero non è primo
ret=false;
//esci
break; //break vuol dire esci dal ciclo, forzatura

return ret;



if (condizione)
    //statement 1
    //blocco è uno statement
    (
        //insieme di operazioni
    )
    else
        statement 2





        tot=prodotto(1,2); //1*2

        prodotto(5,3);

        function int prodotto(int var1, int var 2)
        (
            //se abbiamo a disposizione solo + e - come possiamo scrivere la funzione prodotto?
            int ret=0;

            for(int n= ; n < var2; n++)
            (
                ret=ret+var1;
            )


            return ret;

        )




        prodotto tra due numeri=sommare il primo numero tante volte quante il secondo

        prodotto(5,3) =>

        ret=0;

        ret=ret+5
                ret=ret+5
                        ret=ret+5
        
        restituisco ret;


        int a=1;
        int b=2;

        //scrivere una sequenza di istruzioni che alla fine sposti il valore di a in b e viceversa
        
        //alla fine devo avere a=2 e b=1;